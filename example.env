HOST=""
FASTAPI_PORT="

ENVIRONMENT=""
GUNICORN_WORKERS = ""
DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=

# =====================================================
# OpenAI Model Configuration
# =====================================================
# All models default to gpt-5-mini if not specified
# You can override each agent/model individually if needed

# General OpenAI Configuration
OPENAI_API_KEY=""

# Agent-specific models (Agents SDK) - using gpt-4o-mini for faster responses
NLID_AGENT_MODEL="gpt-4o-mini"          # Natural Language Intent Detection
NLG_AGENT_MODEL="gpt-4o-mini"           # Natural Language Generation
RECIPE_AGENT_MODEL="gpt-4o-mini"        # Recipe Search Agent
NUTRITIONAL_AGENT_MODEL="gpt-4o-mini"   # Nutritional Information Agent
ORCHESTRATOR_AGENT_MODEL="gpt-4o-mini"  # Main Orchestrator Agent
COOKING_GUARDRAIL_MODEL="gpt-4o-mini"   # Cooking-related Guardrail

# SQL Generation Model
SQL_GENERATOR_MODEL="gpt-4o-mini"       # SQL Query Generation
